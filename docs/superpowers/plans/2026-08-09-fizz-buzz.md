# Fizz Buzz 구현 계획

**목표:** 테스트된 Python Fizz Buzz 풀이를 추가하고 원격 `main`에 푸시한다.

**구조:** `Solution.fizzBuzz`가 1부터 `n`까지 순회하며 각 숫자에 맞는 문자열을 목록에 넣는다. 테스트는 공개 메서드를 직접 호출한다.

**기술:** Python 3 표준 라이브러리, `unittest`, Git

## 공통 조건

- 리트코드 `class Solution` 형식을 지킨다.
- 15, 3, 5의 배수를 올바른 우선순위로 처리한다.
- 외부 라이브러리를 추가하지 않는다.

### 작업 1: 실패하는 테스트 추가

**파일:** `tests/test_fizz_buzz.py`

- [ ] 숫자, 3의 배수, 5의 배수, 15의 배수를 검증하는 테스트를 작성한다.
- [ ] `python3 -m unittest discover -s tests -p 'test_fizz_buzz.py' -v`를 실행해 풀이 모듈 부재로 실패하는지 확인한다.

### 작업 2: 풀이와 README 추가

**파일:** `python/easy/fizz_buzz.py`, `README.md`

- [ ] 15, 3, 5의 배수 순서로 조건을 확인하는 `Solution.fizzBuzz`를 작성한다.
- [ ] README에 Fizz Buzz 풀이 링크를 추가한다.

### 작업 3: 검증과 푸시

**파일:** 새 풀이, 테스트, README, 설계·계획 문서

- [ ] `python3 -m unittest discover -s tests -v && git diff --check`로 전체 검증한다.
- [ ] 관련 파일만 스테이징해 `feat: add fizz buzz solution` 커밋을 만들고 `git push origin main`을 실행한다.
